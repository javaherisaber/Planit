<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppThemeParent"/>

    <!-- SplashScreenTheme -->
    <style name="SplashScreenTheme" parent="SplashScreenParent"/>

    <style name="SplashScreenParent" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_screen_bg</item>
    </style>
    <!-- End of SplashScreenTheme -->
</resources>
