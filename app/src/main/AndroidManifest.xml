<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ir.logicbase.planit.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="ir.logicbase.planit.app.ui.base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning"
        tools:targetApi="m">
        <!-- Meta -->
        <meta-data
            android:name="pushe_token"
            android:value="ir.logicbase.planit.app@974974494791" />
        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_APP"
            android:value="true" />
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="621ce0d4431f655a756ffff7842b08497289af54" />
        <!-- End of Meta -->

        <!-- Activity -->
        <activity
            android:name="ir.logicbase.planit.app.ui.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- End of Activity -->

        <!-- Receiver -->

        <!-- End of Receiver -->
    </application>

</manifest>